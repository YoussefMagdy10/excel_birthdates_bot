name: Birthday Monthly Reminder

on:
  schedule:
    - cron: "0 8 1 * *" # 10:00 AM Egypt time approximately, depending on DST/UTC offset
  workflow_dispatch:

jobs:
  send_monthly:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        institution_config:
          # - institutions/smouha_osretna.json
          - institutions/sporting_osretna.json

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run monthly reminder
        env:
          # SMOUHA_OSRETNA_SENDER_PASS: ${{ secrets.SMOUHA_OSRETNA_SENDER_PASS }}
          SPORTING_OSRETNA_SENDER_PASS: ${{ secrets.SPORTING_OSRETNA_SENDER_PASS }}
        run: |
          python -m scripts.run monthly institutions/sporting_osretna.json